<!DOCTYPE html>
<html lang="pl">
<head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>{{ config('app.name') }} | Przypomnij hasło</title><link rel="icon" href="{{ asset('favicon.svg') }}" type="image/svg+xml">@vite(['resources/css/app.css', 'resources/js/app.js'])</head>
<body><main class="container py-5"><div class="row justify-content-center"><div class="col-12 col-md-7 col-lg-5"><a class="brand d-flex align-items-center gap-2 mb-5" href="{{ route('home') }}"><span class="brand-mark">LP</span><span>#LechTYPER</span></a><section class="bg-white border rounded-3 p-4 p-md-5 shadow-sm"><p class="eyebrow mb-2">Odzyskiwanie dostępu</p><h1 class="font-display h3 mb-2">Przypomnij hasło</h1><p class="text-muted-custom mb-4">Podaj email przypisany do konta. Wyślemy link do ustawienia nowego hasła.</p>@if (session('status'))<div class="alert alert-success py-2 small">{{ session('status') }}</div>@endif@if ($errors->any())<div class="alert alert-danger py-2 small">{{ $errors->first() }}</div>@endif<form method="POST" action="{{ route('password.email') }}">@csrf<div class="mb-3"><label class="form-label small" for="email">Email</label><input class="form-control" id="email" name="email" type="email" required autofocus></div><button class="btn btn-primary w-100">Wyślij link</button></form><a class="d-block text-center small mt-3" href="{{ route('login') }}">Wróć do logowania</a></section></div></div></main></body>
</html>
